<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title || 'TukiPlan · login to plan weekends' %></title>
  
  <!-- Tailwind + Inter font style -->
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    /* consistent with TukiPlan identity – calm, modest, friendly */
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    }
    .soft-shadow {
      box-shadow: 0 8px 30px rgba(0,0,0,0.02), 0 2px 6px rgba(0,20,30,0.02);
    }
    .hover-lift:hover {
      transform: translateY(-2px);
      box-shadow: 0 20px 28px -10px rgba(0,45,60,0.06);
    }
    .input-ring {
      transition: all 0.15s ease;
    }
    .input-ring:focus {
      box-shadow: 0 0 0 3px rgba(30, 64, 175, 0.06);
      border-color: #a5b4fc;
    }
  </style>
</head>
<body class="bg-stone-50 text-stone-800 antialiased flex flex-col min-h-screen">

  <!-- NAVBAR – same as homepage, quiet & consistent -->
  <nav class="bg-white/90 backdrop-blur-sm border-b border-stone-200/60 text-stone-700 sticky top-0 z-20">
    <div class="container mx-auto px-5 md:px-6 py-4 flex justify-between items-center">
      <a href="/" class="text-2xl md:text-3xl font-light tracking-tight text-stone-800 flex items-end gap-1">
        <span class="font-semibold text-blue-900">TukiPlan</span>
        <span class="text-xs uppercase bg-stone-100 text-stone-600 px-2 py-1 rounded-full font-medium border border-stone-200/50">weekend</span>
      </a>
      
      <div class="hidden md:flex items-center space-x-8 text-sm font-medium">
        <a href="/events" class="text-stone-600 hover:text-blue-900 transition-colors flex items-center gap-1.5">
          <i class="far fa-compass text-xs"></i> Discover
        </a>
        <a href="/meetups" class="text-stone-600 hover:text-blue-900 transition-colors flex items-center gap-1.5">
          <i class="fas fa-user-group text-xs"></i> Plan together
        </a>
        <a href="/login" class="text-blue-900 border-b-2 border-blue-900/30 pb-1 font-medium">Login</a>
        <a href="/register" class="bg-blue-900 text-white px-5 py-2 rounded-full hover:bg-blue-800 transition shadow-sm text-sm font-medium">Sign up</a>
      </div>

      <button class="md:hidden text-stone-600 p-2 rounded-full hover:bg-stone-100" id="mobile-menu-btn">
        <i class="fas fa-bars text-xl"></i>
      </button>
    </div>
  </nav>

  <!-- LOGIN SECTION – calm, focused, generous whitespace -->
  <main class="flex-grow flex items-center justify-center py-16 px-5">
    <div class="w-full max-w-md">
      
      <!-- subtle back link -->
      <a href="/index" class="inline-flex items-center gap-1 text-sm text-stone-500 hover:text-stone-700 mb-8 transition group">
        <i class="fas fa-arrow-left text-xs group-hover:-translate-x-1 transition-transform"></i> back to home
      </a>

      <!-- login card – soft, elevated, modest -->
      <div class="bg-white/90 backdrop-blur-sm rounded-3xl soft-shadow border border-stone-100/80 p-8 md:p-10">
        
        <!-- brand micro-message -->
        <div class="flex justify-center mb-6">
          <span class="inline-flex items-center gap-2 bg-stone-100 text-stone-700 px-4 py-2 rounded-full text-xs uppercase tracking-wider border border-stone-200/60">
            <i class="fas fa-hand-peace text-stone-500"></i> welcome back
          </span>
        </div>
        
        <h1 class="text-3xl md:text-4xl font-light text-stone-800 mb-2 text-center tracking-tight">
          Log in to <span class="font-semibold text-blue-900">TukiPlan</span>
        </h1>
        <p class="text-stone-500 text-sm text-center mb-10 max-w-xs mx-auto">
          Ready to plan this weekend? Sign in with your email.
        </p>
        
        <!-- login form – simple, no clutter -->
        <form action="/login" method="POST" class="space-y-6">
          
          <!-- email field -->
          <div class="space-y-2">
            <label for="email" class="block text-sm font-medium text-stone-700 pl-1">
              Email address
            </label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                <i class="fas fa-envelope text-stone-400 text-sm"></i>
              </div>
              <input 
                type="email" 
                id="email" 
                name="email" 
                placeholder="you@example.com" 
                class="w-full pl-11 pr-4 py-4 bg-stone-50/80 border border-stone-200 rounded-2xl text-stone-700 placeholder:text-stone-400 text-base focus:outline-none input-ring transition"
                required
              />
            </div>
          </div>

          <!-- password field -->
          <div class="space-y-2">
            <div class="flex justify-between items-center pl-1">
              <label for="password" class="block text-sm font-medium text-stone-700">
                Password
              </label>
              <a href="/forgot-password" class="text-xs text-stone-500 hover:text-blue-800 transition">
                Forgot?
              </a>
            </div>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                <i class="fas fa-lock text-stone-400 text-sm"></i>
              </div>
              <input 
                type="password" 
                id="password" 
                name="password" 
                placeholder="••••••••" 
                class="w-full pl-11 pr-12 py-4 bg-stone-50/80 border border-stone-200 rounded-2xl text-stone-700 placeholder:text-stone-400 text-base focus:outline-none input-ring transition"
                required
              />
              <button type="button" class="absolute inset-y-0 right-0 pr-4 flex items-center text-stone-400 hover:text-stone-600" onclick="togglePasswordVisibility()">
                <i class="fas fa-eye text-sm" id="togglePasswordIcon"></i>
              </button>
            </div>
          </div>

          <!-- remember me – subtle checkbox -->
          <div class="flex items-center justify-between pt-2">
            <div class="flex items-center gap-2">
              <input 
                type="checkbox" 
                id="remember" 
                name="remember" 
                class="w-4 h-4 rounded border-stone-300 text-blue-900 focus:ring-blue-200 focus:ring-offset-0 bg-stone-50"
              />
              <label for="remember" class="text-sm text-stone-600">Remember me</label>
            </div>
          </div>

          <!-- submit button – modest, confident -->
          <button 
            type="submit" 
            class="w-full bg-blue-900 hover:bg-blue-800 text-white font-medium px-6 py-4 rounded-full transition text-base shadow-sm flex items-center justify-center gap-3 group mt-8"
          >
            <span>Log in to TukiPlan</span>
            <i class="fas fa-arrow-right text-xs group-hover:translate-x-1 transition-transform"></i>
          </button>
        </form>

        <!-- divider – clean and simple -->
        <div class="relative my-8">
          <div class="absolute inset-0 flex items-center">
            <div class="w-full border-t border-stone-200"></div>
          </div>
          <div class="relative flex justify-center text-xs uppercase">
            <span class="bg-white px-6 text-stone-500">or</span>
          </div>
        </div>

        <!-- sign up link – quiet invitation -->
        <p class="text-center text-sm text-stone-600">
          New to TukiPlan? 
          <a href="/register" class="font-medium text-blue-900 hover:text-blue-800 transition border-b border-blue-200 hover:border-blue-400 pb-0.5">
            Create an account
          </a>
        </p>

        <!-- demo hint – subtle, helpful (can be removed in production) -->
        <p class="text-center text-xs text-stone-400 mt-8 pt-2 border-t border-stone-100">
          <i class="fas fa-shield-alt mr-1"></i> demo: tuki@example.com / weekend123
        </p>
      </div>
      
      <!-- quiet note about TukiPlan community -->
      <p class="text-center text-xs text-stone-400 mt-10 max-w-xs mx-auto">
        By logging in, you agree to our gentle terms and privacy promise. 
        No spam, just weekend vibes.
      </p>
    </div>
  </main>

  <%- include("./partials/footer.ejs")  %>

  

  <!-- tiny script for password toggle (elegant, modest) -->
  <script>
    function togglePasswordVisibility() {
      const passwordInput = document.getElementById('password');
      const icon = document.getElementById('togglePasswordIcon');
      if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        icon.classList.remove('fa-eye');
        icon.classList.add('fa-eye-slash');
      } else {
        passwordInput.type = 'password';
        icon.classList.remove('fa-eye-slash');
        icon.classList.add('fa-eye');
      }
    }

    // mobile menu placeholder (consistent with homepage)
    document.getElementById('mobile-menu-btn')?.addEventListener('click', function() {
      // you can connect this to a drawer if needed — keeping it minimal
      alert('Menu — ready for your mobile implementation 🏕️');
    });
  </script>
</body>
</html>